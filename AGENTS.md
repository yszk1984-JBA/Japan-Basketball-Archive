# AI作業ルール — Governance v1.0

作業前にREADME.mdとdocs/内の関連文書を読む。OwnerはYuichi。日本語で変更内容、確認結果、残課題を簡潔に説明する。

## 必須原則

- AIはMaster Dataを直接承認・確定しない。AIがYuichiの承認を代筆・推定してはならない。
- 不明情報を推測しない。出典がない値、矛盾、人物同定の未解決事項は候補として保留する。
- 事実にはSourceを持たせ、どの項目をどの出典箇所が裏付けるか追跡できるようにする。
- RAW → CANDIDATE → QA → VERIFIED → HUMAN APPROVAL → MASTER を省略しない。
- VERIFIEDをMASTERとして扱わない。Master反映はYuichiの明示的承認の対象・版・範囲内でのみ行う。
- 原資料を上書き・削除しない。Excelの既存ラベルを承認の証拠として自動採用しない。

## 作業範囲

- 現在はGovernance v1.0とデータ設計v0.1の文書整備のみ。
- 大規模実装、スキーマ拡張、データ一括取込は別途Yuichiと範囲を合意してから行う。
- 既存の変更を保持し、必要なファイルだけ変更する。
- パスワード・トークンをファイルや履歴に記録しない。
- 文書のリンクと整合性、実装時は変更に応じた検証を確認し、未実行の検証を成功と記載しない。
- commitとpushの意味を簡潔に説明し、対象差分を確認する。Gitへの保存はMaster承認ではない。

担当と引継ぎはdocs/AI_WORKFLOW.md、承認記録はdocs/DATA_POLICY.mdに従う。
